apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: main-ingress
  namespace: udemx
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
spec:
  ingressClassName: nginx
  tls:
  - hosts:
    - myapp.udemx.local
    - app.udemx.local
    secretName: tls-secret
  rules:
  - host: myapp.udemx.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: hello-udemx-service
            port:
              number: 80
  - host: app.udemx.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: incident-logger-incident-logger-frontend
            port:
              number: 80